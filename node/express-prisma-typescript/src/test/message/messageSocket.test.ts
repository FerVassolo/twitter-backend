

// no debería testear los guardados a la db, sino los eventos
// Should test the events, not the db saves, that is the job of the service tests


describe("Message Socket", () => {
  it('should pass', () => {
    expect(true).toBe(true);
  });
})


describe("Message Socket", () => {
  it('should pass', () => {
    expect(true).toBe(true);
  });
})
